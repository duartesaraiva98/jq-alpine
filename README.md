# Docker image of [jq](https://github.com/stedolan/jq) on alpine

#### To build docker image:
1. Download the desired release zip file from jq repository 
2. Extract folder
3. Rename it to jq
4. Run `docker build .`